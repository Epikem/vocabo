# backend of memorybook

To run this project, rename `template.env` file to `.env` and fill fields with proper value. You have to be a member of Naver to get Naver api but you can run without the api.